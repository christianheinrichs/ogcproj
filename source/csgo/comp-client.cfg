// My Counter-Strike: Global Offensive competitive configuration
// Last modified on 19 February 2022

// Custom keybinds

// Bind microphone PTT key to F for quick access
bind "f" "+voicerecord"

// Grenade specific binds to make them more accessible in competitive mode.
// This way you won’t need to cycle through them with the 4 key for example.
// These binds are also intentionally placed close to the WASD movement keys.
// In this case, assume a QWERTY or QWERTZ keyboard layout
bind "4" "use weapon_smokegrenade"
bind "6" "use weapon_flashbang"
bind "c" "use weapon_hegrenade"
bind "v" "use weapon_incgrenade; use weapon_molotov;"
bind "x" "use weapon_decoy"

// Keypad buybinds

// No modifications here, so these are simply derived from the keypad buybinds
// defined in autoexec.cfg

// Custom settings

// Again, no modifications

// In-game menu section

// Settings menu > Game settings > Video > Video

// Resolution
// Slightly lower than native 1080p resolution to make things more compact
// Refer to video-comp.txt
// Width: setting.defaultres 1768
// Height: setting.defaultresheight 992

// Display mode
// Use windowed mode instead of fullscreen window
// Refer to video-comp.txt
// Windowed: setting.fullscreen 0, setting.nowindowborder 0
// Fullscreen: setting.fullscreen 1, setting.nowindowborder 0
// Fullscreen windowed: setting.fullscreen 0, setting.nowindowborder 1

// Settings menu > Game settings > Video > Advanced video

// No modifications

// Settings menu > Game settings > Video > HUD edge positions

// No modifications

// Settings menu > Game settings > Audio > Audio

// No modifications

// Settings menu > Game settings > Audio > Music

// In order to fully focus on a competitive match, turn off music completely
// with the exception of the ten second warning volume

// Main menu volume
snd_menumusic_volume "0"

// Round start volume
snd_roundstart_volume "0"

// Round end volume
snd_roundend_volume "0"

// Bomb/Hostage volume
snd_mapobjective_volume "0"

// Ten second warning volume
snd_tensecondwarning_volume "1"

// Death camera volume
snd_deathcamera_volume "0"

// MVP volume
snd_mvp_volume "0"

// Settings menu > Game settings > Game > Game

// This can be used for faster localisation of the objective, so enable it
// Enable game instructor messages
// Yes: gameinstructor_enable 1
// No: gameinstructor_enable 0
gameinstructor_enable "1"

// Settings menu > Game settings > Game > HUD

// No modifications

// Settings menu > Game settings > Game > Team

// No modifications

// Settings menu > Game settings > Game > Communication

// No modifications

// Settings menu > Game settings > Game > Spectator/Scoreboard

// No modifications

// Settings menu > Game settings > Game > Item

// No modifications

// Settings menu > Game settings > Game > Radar/Tablet

// No modifications

// Settings menu > Game settings > Game > Crosshair

// No modifications

// Settings menu > Game settings > Keyboard/Mouse > Keyboard & mouse settings

// No modifications

// Settings menu > Game settings > Keyboard/Mouse > Movement keys

// No modifications

// Settings menu > Game settings > Keyboard/Mouse > Weapon keys

// Inspect weapon

// Key/Button: Not bound

// Cycle grenades

// Key/Button: Not bound

// HE grenade

// Key/Button: Not bound

// Healthshot

// Key/Button: Not bound

// Settings menu > Game settings > Keyboard/Mouse > UI keys

// No modifications

// Settings menu > Game settings > Keyboard/Mouse > Communication options

// Use microphone

// Key/Button: Refer to custom binds in the beginning of file

// Settings menu > Game settings > Keyboard/Mouse > Chat wheel keys

// No modifications

// Settings menu > Game settings > Controller > Controller

// No modifications

// Settings menu > Game settings > Controller > Controller bindings

// No modifications

echo "*** Counter-Strike: Global Offensive competitive configuration loaded ***"
